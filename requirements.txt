# Core agent pipeline (agent/*, main.py) has zero external dependencies (stdlib only).
# Optional, only needed if you set ANTHROPIC_API_KEY to enable LLM-augmented extraction:
anthropic>=0.40.0

# Phase 4: FastAPI backend (backend/*)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# Testing
pytest>=8.3.0
httpx>=0.27.0
